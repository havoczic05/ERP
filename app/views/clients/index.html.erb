<div class="page-head"><h1>Clientes</h1></div>

<div class="toolbar">
  <%= form_with url: clients_path, method: :get, local: true, class: "filters" do |f| %>
    <div class="field">
      <%= f.label :q, 'Buscar' %>
      <%= f.text_field :q, value: params[:q], placeholder: 'Nombre o número de documento' %>
    </div>
    <%= f.submit 'Buscar' %>
  <% end %>

  <%= action_link 'Nuevo cliente', new_client_path, variant: :primary, size: :md, icon: :plus %>
</div>

<table>
  <thead>
    <tr>
      <th>Nombre completo</th>
      <th>Tipo de documento</th>
      <th>Número de documento</th>
      <th>Teléfono</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <%= render @clients %>
  </tbody>
</table>

<% if @clients.empty? %>
  <p id="no-clients-message">No se encontraron clientes.</p>
<% end %>

<%= pagination_nav(@pagy) %>
